#include <bits/stdc++.h>
using namespace std;
void learn_number()
{
    //少数点以下の桁数指定
    int num = 8;
    cout << fixed << setprecision(num) << endl;

    //累乗の計算
    int exponentiation = 2;
    int square_num = pow(3, exponentiation);

    // 2乗根
    // sqrt(3)
    // 3乗根
    // cbrt(3)

    // 平均の計算
    // double ave = accumulate(num_vecotr.begin(), num_vector.end(), 0.0) / num_vector.size();

    //
}